<div>
<h2>Loadmore</h2>

<p>
将该组件放在页面底部，当页面滚动到底部时，触发 `reachBottom` 事件，`visible` 属性表示组件是否可见，当数据已加载完所有数据时，组件应该不可见，此时页面滚动到底部不再触发 `reachBottom` 事件
</p>

<smart-code :code="htmlcode" lang="html" />
<smart-code :code="jscode" lang="js" />
</div>
