<div>
<h2>Loadmore</h2>

<p>
将该组件放在页面底部，当页面滚动到底部时，触发 <code>reachBottom</code> 事件，<code>allLoaded</code> 属性表示数据已加载完所有数据，当 <code>allLoaded</code> 为 <code>true</code> 时，页面滚动到底部不再触发事件
</p>

<smart-code :code="htmlcode" lang="html" />
<smart-code :code="jscode" lang="js" />
</div>
